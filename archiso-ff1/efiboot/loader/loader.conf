default archiso-x86_64-linux-auto-installation.conf
timeout 0
# Keep the live-media ESP read-only during boot for the same reason as the
# installed system (issue #122): no random-seed rewrite window on FAT32.
random-seed-mode off
