# No getty is ever spawned by switching VTs (ffos#126): the only console on an
# installed FF1 is the explicitly enabled getty@tty2 (password login). Kernel
# messages go to tty3 (console=tty3) where no login prompt exists.
[Login]
NAutoVTs=0
ReserveVT=0
