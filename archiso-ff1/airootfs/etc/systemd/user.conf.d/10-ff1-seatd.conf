# cage runs from a `systemd --user` unit with no logind session behind it
# (ffos#126). libseat's logind backend would then pick whatever session logind
# elects for the user (an SSH login, for example) and fail to open a seat, so
# pin the seatd backend for every user unit.
[Manager]
DefaultEnvironment=LIBSEAT_BACKEND=seatd
