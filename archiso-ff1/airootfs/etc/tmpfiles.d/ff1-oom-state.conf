# Create /var/lib/oom_state owned by feralfile so that both the earlyoom hook
# (root) and feral-controld / .start-services.sh (feralfile) can read and write
# the OOM kill counter files.
d /var/lib/oom_state 0755 feralfile feralfile -
